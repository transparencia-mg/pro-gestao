# Pró Gestão - Diretoria Central de Transparência Ativa

### Setup do Projeto

```
# linux
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# windows
python3 -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
```
